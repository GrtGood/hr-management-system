# 🔧 数据库错误修复指南

## 问题诊断

你遇到的 `OperationalError` 是由于**数据库模型循环引用**导致的。已经修复！

---

## ✅ 解决方案（按顺序执行）

### **方案1：使用强力重置工具（推荐）**

#### 步骤1：拉取最新代码

```bash
cd <你的项目目录>
git pull origin pr-3
```

或者手动下载更新后的文件：
- `models.py` （已修复循环引用问题）
- `reset_database.py` （强力重置工具）

#### 步骤2：运行重置脚本

```bash
python reset_database.py
```

#### 步骤3：输入确认

```
⚠️  删除数据库将清空所有数据！确认继续吗？(输入 yes 继续): yes
```

#### 步骤4：等待完成

你会看到：
```
============================================================
  ✅ 数据库重置完成！
============================================================

📊 数据库信息：
   位置: C:\Users\xxx\hr-management-system\hr_system.db
   大小: 32.00 KB

👤 默认管理员账户：
   用户名: admin
   密码: admin123
```

#### 步骤5：启动系统

```bash
python app.py
```

或者双击 `启动脚本.bat`

---

### **方案2：手动修复（如果方案1失败）**

#### 步骤1：完全关闭所有进程

1. **关闭所有Python进程**：
   - Windows: 打开任务管理器，结束所有 `python.exe` 和 `pythonw.exe`
   - Mac: 打开活动监视器，结束所有Python进程
   - Linux: `killall python` 或 `pkill python`

2. **关闭所有浏览器窗口**

3. **关闭可能占用数据库的编辑器**（如VS Code、PyCharm）

#### 步骤2：手动删除数据库文件

找到项目目录下的 `hr_system.db` 文件并删除：

```bash
# Windows
del hr_system.db

# Mac/Linux
rm hr_system.db
```

如果找不到，搜索整个项目：

```bash
# Windows
dir /s *.db

# Mac/Linux
find . -name "*.db"
```

#### 步骤3：更新 models.py

确保 `models.py` 中的 Department 类使用了修复后的版本：

```python
# 关系
employees = db.relationship('Employee', backref='department', foreign_keys='Employee.department_id')
# manager关系通过属性访问，避免循环引用
@property
def manager(self):
    """获取部门负责人"""
    if self.manager_id:
        return Employee.query.get(self.manager_id)
    return None
```

#### 步骤4：重新初始化

```bash
python app.py
```

系统会自动创建新数据库。

---

### **方案3：使用SQLite Browser（最后手段）**

如果以上方法都失败，说明数据库文件严重损坏：

1. **下载 DB Browser for SQLite**：
   https://sqlitebrowser.org/dl/

2. **打开数据库文件** `hr_system.db`

3. **删除所有表**

4. **关闭DB Browser**

5. **运行** `python app.py` 重新创建

---

## 🔍 核心修复内容

### 修复1：models.py 循环引用问题

**之前（有问题）**：
```python
manager = db.relationship('Employee', foreign_keys=[manager_id], post_update=True)
```

**现在（已修复）**：
```python
@property
def manager(self):
    """获取部门负责人"""
    if self.manager_id:
        return Employee.query.get(self.manager_id)
    return None
```

### 修复2：强力重置工具

新增了 `reset_database.py`，功能包括：
- ✅ 自动检测并删除旧数据库
- ✅ 强制关闭所有数据库连接
- ✅ 如果删除失败，自动重命名备份
- ✅ 完整的错误提示和解决建议
- ✅ 详细的初始化日志

---

## 📊 验证修复成功

运行 `python app.py` 后，你应该看到：

```
数据库初始化完成！
管理员账户已创建：用户名 admin，密码 admin123
 * Running on http://0.0.0.0:5000
```

然后访问 `http://localhost:5000` 应该能正常看到登录页面。

---

## ❓ 常见问题

### Q1: 运行reset_database.py报错 "ModuleNotFoundError"

**解决**：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q2: 数据库文件被占用，无法删除

**解决**：
1. 关闭所有Python进程和浏览器
2. 重启电脑
3. 使用方案3的SQLite Browser

### Q3: 删除数据库后，旧数据丢失了

**说明**：这是正常的！重置数据库会清空所有数据。如果需要保留数据，请先备份。

### Q4: 运行app.py后还是报OperationalError

**检查**：
1. 确认 `models.py` 已经更新到最新版本
2. 确认 `hr_system.db` 已被完全删除
3. 查看完整错误信息，发给我进一步诊断

---

## 🎯 下一步

修复完成后，建议：

1. **添加测试数据**：
   - 创建2-3个部门
   - 添加5-10个员工
   - 录入一些考勤和绩效数据

2. **测试新功能**：
   - 首页数据可视化图表
   - 高级搜索功能
   - Excel批量导入

3. **准备演示**：
   - 查看 `快速测试指南.md`
   - 按照测试流程验证所有功能

---

**如果还有问题，请把完整的错误信息发给我！** 🚀
