# 🚀 快速运行指南

## 一键启动（3步）

```bash
# 1. 进入项目目录
cd /project/workspace/hr-management-system

# 2. 运行应用（依赖已安装）
python3 app.py

# 3. 打开浏览器访问
# http://localhost:5000
# 登录账号: admin
# 登录密码: admin123
```

## 成功运行标志

看到以下输出说明成功：

```
管理员账户已创建：用户名 admin，密码 admin123
数据库初始化完成！
 * Running on http://0.0.0.0:5000
```

## 系统功能

登录后即可使用：

1. **员工管理** - 添加、查看、编辑员工信息
2. **部门管理** - 管理部门组织结构
3. **考勤管理** - 记录员工考勤
4. **绩效考核** - 员工绩效评估（自动评级）
5. **职称评审** - 职称申请和审核
6. **培训发展** - 培训记录管理
7. **数据导出** - 导出Excel报表

## 常见问题

### Q: 端口被占用？
修改 `app.py` 最后一行的端口号：
```python
app.run(debug=True, host='0.0.0.0', port=8000)  # 改为8000
```

### Q: 忘记密码？
删除 `hr_system.db` 文件，重新运行即可重置。

### Q: 想添加测试数据？
登录后在各个模块手动添加，或修改 `app.py` 的 `init_database()` 函数添加示例数据。

---

**详细文档请查看**: `README.md`  
**项目总结请查看**: `PROJECT_SUMMARY.md`
